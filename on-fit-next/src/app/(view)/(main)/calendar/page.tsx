import CalendarClientPage from "@/app/(view)/(main)/calendar/components/CalendarClientPage";

export default async function CalendarPage() {

  // 유저가 있으면 클라이언트 컴포넌트 렌더링
  return <CalendarClientPage />;
}